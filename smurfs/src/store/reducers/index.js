const initalState = {
    smurfData: [
        {name:"Brainey",
        age:200,
        height:"5cm",
        id:0}
    ]
}

export default (state = initalState, action) => {
    switch(action.type){
        default:
            return state
    }
}