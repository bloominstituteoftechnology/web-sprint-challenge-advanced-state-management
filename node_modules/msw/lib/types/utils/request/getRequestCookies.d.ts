import { MockedRequest } from '../handlers/requestHandler';
/**
 * Returns relevant document cookies based on the request `credentials` option.
 */
export declare function getRequestCookies(req: MockedRequest): {
    [key: string]: string;
};
